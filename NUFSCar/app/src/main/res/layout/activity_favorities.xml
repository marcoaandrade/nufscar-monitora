<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_favorities"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.monitora.android.nufscar.view.FavoritiesActivity">

    <TextView
        android:id="@+id/textFavorites"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:textSize="10pt"
        android:text="Favoritos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/editTextBF"
        android:textSize="8pt"
        android:hint="Palavra-chave"
        android:layout_width="270sp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textFavorites"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ScrollView
        android:id="@+id/scrollViewFavorite"
        android:layout_below="@+id/editTextBF"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ListView
            android:id="@+id/listViewFavorite"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        </ListView>

    </ScrollView>

    <Button
        android:id="@+id/buttonSearchF"
        android:textSize="8pt"
        android:background="@color/colorPrimary"
        android:hint="Busca"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/scrollViewFavorite"
        android:layout_toRightOf="@+id/editTextBF"
        android:layout_alignTop="@+id/editTextBF" />
</RelativeLayout>
